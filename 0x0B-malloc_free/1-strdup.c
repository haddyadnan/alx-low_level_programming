#include "main.h"
#include <string.h>

/**
 * _strdup - returns pointer to a nwly allocated space
 * @str: string
 * Return: Pointer or NULL
 *
 */

char *_strdup(char *str)
{
	return (strdup(str));
}
