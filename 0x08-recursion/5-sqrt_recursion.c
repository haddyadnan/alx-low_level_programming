#include "main.h"
#include <math.h>
/**
 * _sqrt_recursion - find sqrt
 *
 * @n:
 * Return: int
 */

int _sqrt_recursion(int n)
{
	int sqr = (int)sqrt(n);
	int value = (int)(sqr);

	return (value);
}
